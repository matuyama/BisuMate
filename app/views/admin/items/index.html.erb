<div class="container">
      <h4><strong>商品一覧</strong></h4>
  <div class="row">
  </div>

  <div class="row">
    <div class="col">
      <%= link_to "新規商品登録", new_admin_item_path, class: "btn btn-primary" %>
      <table class="table table-bordered">
        <thead>
          <tr>
            <th>商品ID</th>
            <th>商品名</th>
            <th>税抜価格</th>
            <th>ジャンル</th>
            <th>ステータス</th>
            <th>在庫数</th>
          </tr>
        </thead>
      <% @items.each do |item| %>
        <tbody>
          <tr>
            <td><%= item.id %></td>
            <td><%= link_to item.name, admin_item_path(item) %></td>
            <td><%= item.price_tax_excluded %></td>
            <td><%= item.genre.name %></td>
            <td><%= render "is_on_sale", item: item %></td>
            <td><%= item.stock.to_s(:delimited) %></td>
          </tr>
        </tbody>
      <% end %>
      </table>
    </div>
  </div>
</div>